# 최솟값 구하기

arr=[5,3,7,9,2,5,2,6]
addMin = float('inf') #arrMin가장 큰값으로 지정
for i in range(len(arr)):
  if (arr[i]<addMin):
    addMin = arr[]

# 아래와 같이 addMin을 첫번째 arr의 elt로 지정했으면
    #range(1,len(arr))해주어야함

arr=[5,3,7,9,2,5,2,6]
addMin = arr[0] #arrMin가장 큰값으로 지정
for i in range(1,len(arr)):
  if (arr[i]<addMin):
    addMin = arr[]


###############

arr=[5,3,7,9,2,5,2,6]
addMin = float('inf') #arrMin가장 큰값으로 지정
for x in arr:
  if (x<addMin):
    addMin = x
